
<nav class="mt-2">
    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
        <!-- Add icons to the links using the .nav-icon class
        with font-awesome or any other icon font library -->
        @if (AdminBaseModel.Menu != null && AdminBaseModel.Menu.Count > 0)
        {
            foreach (var item in AdminBaseModel.Menu)
            {
                if (item.HasChild)
                {
                    <li class="nav-item has-treeview menu-open">
                        <a href="#" class="nav-link">
                            <i class="nav-icon @item.Icon"></i>
                            <p>
                                <span class="brand-text font-weight-bold">@item.Name</span>
                                <i class="right fas fa-angle-left"></i>
                            </p>
                        </a>
                        <ul class="nav nav-treeview">
                            @foreach (var child in item.ChildItems!)
                            {
                                <li class="nav-item">
                                    <a href="@child.Link" class="nav-link">
                                        <i class="@child.Icon"></i>
                                        <span class="brand-text font-weight-bold">@child.Name</span>
                                    </a>
                                </li>
                            }
                        </ul>
                    </li>
                }
                else
                {
                    <li class="nav-item border-bottom">
                        <a href="@item.Link" class="nav-link">
                            <h5 class="text-white">
                                <i class="@item.Icon"></i>
                                <span class="brand-text font-weight-bold">@item.Name</span>
                            </h5>
                        </a>
                    </li>
                }
            }
        }
    </ul>
</nav>

